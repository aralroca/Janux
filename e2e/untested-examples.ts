/**
 * Examples with no dedicated e2e suite beyond the shared smoke test.
 *
 * This is a backlog, not an allowlist: entries are debts to pay, and the goal
 * is for this array to stay empty. Adding an example without an e2e suite
 * requires listing it here on purpose — silence is not an option.
 */
export const UNTESTED_EXAMPLES: string[] = [];
