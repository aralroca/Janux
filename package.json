{
  "name": "janux-monorepo",
  "version": "0.3.0",
  "description": "Janux \u2014 the agent-native fullstack UI framework. One component, two faces.",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aralroca/Janux.git"
  },
  "license": "MIT",
  "author": {
    "name": "Aral Roca Gomez",
    "email": "contact@aralroca.com"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test packages/janux packages/janux-server packages/janux-agent packages/janux-vite packages/janux-cli packages/janux-tailwind packages/conformance packages/docs-tests apps/docs",
    "test:e2e": "bun test e2e",
    "test:coverage": "bun test packages/janux packages/janux-server packages/janux-agent packages/janux-vite packages/janux-cli packages/janux-tailwind packages/conformance packages/docs-tests apps/docs --coverage",
    "test:census": "bun scripts/test-census.ts",
    "typecheck": "for p in janux janux-server janux-agent janux-vite janux-cli conformance; do bunx tsc --noEmit -p packages/$p || exit 1; done",
    "dev:shop": "bun run --cwd examples/shop dev",
    "dev:i18n": "bun run --cwd examples/i18n dev",
    "dev:web-agent": "bun run --cwd examples/with-web-agent dev",
    "release": "bun scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
