# @janux/vite

Vite plugin for [Janux](https://github.com/aralroca/Janux): JSX runtime config, SSR dev bridge, and the SWC transform that swaps `*.api.ts` imports for ~100-byte fetch stubs so server code never reaches client bundles.
