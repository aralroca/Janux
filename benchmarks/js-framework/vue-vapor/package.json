{
	"name": "vue-vapor-jsbench",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"description": "js-framework-benchmark fixture for Vue Vapor (3.6 vapor mode: <script setup vapor> SFC, keyed v-for over a shallowRef row array) - the Vue Vapor column for the 1000-row create/update/swap/select/remove ops plus the keyed-reorder matrix (reverse/shuffle/rotate/displace).",
	"scripts": {
		"dev": "vite --host --port 5180 --strictPort",
		"build": "vite build",
		"preview": "vite preview --port 5180 --strictPort"
	},
	"dependencies": {
		"vue": "3.6.0-rc.1",
		"@vue/runtime-dom": "3.6.0-rc.1",
		"@vue/runtime-vapor": "3.6.0-rc.1"
	},
	"devDependencies": {
		"@vitejs/plugin-vue": "^6.0.8",
		"vite": "^8.1.5"
	}
}
