{
	"name": "react-jsbench",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"description": "js-framework-benchmark fixture for React 19 (canonical keyed react-hooks impl) - the reference React column for the 1000-row create/update/swap/select/remove ops.",
	"scripts": {
		"dev": "vite --host --port 5175 --strictPort",
		"build": "vite build",
		"preview": "vite preview --port 5175 --strictPort"
	},
	"dependencies": {
		"react": "^19.2.7",
		"react-dom": "^19.2.7"
	},
	"devDependencies": {
		"@vitejs/plugin-react": "^6.0.3",
		"vite": "^8.1.5",
		"terser": "^5.49.0"
	}
}
