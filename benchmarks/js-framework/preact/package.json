{
	"name": "preact-jsbench",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"description": "js-framework-benchmark fixture for Preact (keyed hooks implementation).",
	"scripts": {
		"dev": "vite --host --port 5260 --strictPort",
		"build": "vite build",
		"preview": "vite preview --port 5260 --strictPort"
	},
	"dependencies": {
		"preact": "^10.29.7"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.10.6",
		"vite": "^8.1.5",
		"terser": "^5.49.0"
	}
}
