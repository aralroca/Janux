{
	"name": "preact-news-bench",
	"private": true,
	"type": "module",
	"version": "0.0.0",
	"description": "News-site SSR and hydration benchmark fixture for Preact.",
	"scripts": {
		"dev": "vite --host --port 5270 --strictPort"
	},
	"dependencies": {
		"preact": "^10.29.7",
		"preact-render-to-string": "^6.7.0"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.10.6",
		"vite": "^8.1.5"
	}
}
